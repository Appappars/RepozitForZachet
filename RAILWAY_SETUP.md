# Настройка Railway для работы с PostgreSQL

## Проблема: ECONNREFUSED ::1:5432

Эта ошибка означает, что `DATABASE_URL` не установлен или не передается в Web Service.

## Решение (пошаговая инструкция):

### Шаг 1: Убедитесь, что PostgreSQL добавлен

1. Откройте ваш проект на Railway
2. Проверьте, есть ли сервис "PostgreSQL" в списке сервисов
3. Если нет - нажмите "+ New" → "Database" → "Add PostgreSQL"

### Шаг 2: Проверьте связь между сервисами

**ВАЖНО:** PostgreSQL и Web Service должны быть в **ОДНОМ проекте**!

1. Убедитесь, что оба сервиса находятся в одном проекте Railway
2. Если они в разных проектах - переместите их в один

### Шаг 3: Проверьте переменные окружения

1. **Откройте PostgreSQL сервис:**
   - Нажмите на PostgreSQL сервис
   - Перейдите в "Variables"
   - Найдите `DATABASE_URL` или `POSTGRES_URL`
   - Скопируйте значение

2. **Откройте Web Service:**
   - Нажмите на ваш Web Service (бэкенд)
   - Перейдите в "Variables"
   - Проверьте, есть ли `DATABASE_URL`

3. **Если DATABASE_URL отсутствует:**
   - Нажмите "+ New Variable"
   - Name: `DATABASE_URL`
   - Value: вставьте скопированное значение из PostgreSQL сервиса
   - Нажмите "Add"

### Шаг 4: Альтернативный способ (через Reference)

Railway может автоматически связать переменные через References:

1. В Web Service → "Variables"
2. Нажмите "+ New Variable"
3. Выберите "Reference"
4. Выберите PostgreSQL сервис
5. Выберите переменную `DATABASE_URL` или `POSTGRES_URL`
6. Сохраните

### Шаг 5: Перезапустите сервис

1. В Web Service нажмите "Deployments"
2. Нажмите "Redeploy" на последнем деплое
3. Или сделайте новый push в GitHub

### Шаг 6: Проверьте логи

После перезапуска проверьте логи:

1. Откройте Web Service → "Deployments" → последний деплой → "View Logs"
2. Должно быть:
   - `DATABASE_URL: установлен`
   - `Подключение к базе данных установлено`
   - `✅ База данных инициализирована успешно`

## Если проблема сохраняется:

### Вариант 1: Ручная установка DATABASE_URL

1. Откройте PostgreSQL сервис → "Connect" → "Postgres Connection URL"
2. Скопируйте полный URL (начинается с `postgresql://`)
3. В Web Service → "Variables" → добавьте:
   - Name: `DATABASE_URL`
   - Value: `postgresql://...` (скопированный URL)

### Вариант 2: Проверьте формат URL

DATABASE_URL должен быть в формате:
```
postgresql://user:password@host:port/database
```

Или:
```
postgres://user:password@host:port/database
```

### Вариант 3: Создайте новый проект

Если ничего не помогает:

1. Создайте новый проект Railway
2. Добавьте PostgreSQL первым
3. Затем добавьте Web Service из GitHub
4. Railway автоматически свяжет их

## Проверка работы:

После настройки в логах должно быть:

```
🚀 Запуск сервера...
DATABASE_URL: установлен
Попытка подключения к базе данных...
Подключение к базе данных установлено
База данных доступна
Таблица records создана/проверена
Индексы созданы/проверены
✅ База данных инициализирована успешно
✅ Сервер запущен на порту 5000
```

## Дополнительная информация:

- Railway автоматически связывает сервисы в одном проекте
- DATABASE_URL должен быть доступен в Web Service
- Если переменная не передается автоматически - добавьте её вручную
- Формат URL важен - должен начинаться с `postgresql://` или `postgres://`


# Быстрый старт с PostgreSQL

## Развертывание на Railway (5 минут)

### 1. Создайте PostgreSQL базу данных

В вашем проекте Railway:
- Нажмите "+ New" → "Database" → "Add PostgreSQL"
- Railway автоматически создаст базу и добавит `DATABASE_URL`

### 2. Деплой приложения

```bash
git add .
git commit -m "Добавлена поддержка PostgreSQL"
git push
```

Railway автоматически:
- Установит зависимости
- Соберет фронтенд
- Подключится к PostgreSQL
- Создаст таблицы при первом запуске

### 3. Готово!

Ваше приложение работает с PostgreSQL! 🎉

## Локальная разработка

### Вариант 1: Docker (самый простой)

```bash
# Запустите PostgreSQL
docker run --name postgres-auto-app \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=auto_app \
  -p 5432:5432 -d postgres

# Создайте .env в server/
echo "DATABASE_URL=postgresql://postgres:password@localhost:5432/auto_app" > server/.env

# Запустите сервер
cd server
npm install
npm start
```

### Вариант 2: Локальная установка

1. Установите PostgreSQL: https://www.postgresql.org/download/
2. Создайте базу: `createdb auto_app`
3. Создайте `server/.env`:
   ```
   DATABASE_URL=postgresql://username:password@localhost:5432/auto_app
   ```
4. Запустите: `cd server && npm install && npm start`

## Проверка работы

1. Откройте приложение
2. Создайте запись
3. Проверьте логи - должно быть: "База данных инициализирована успешно"

## Миграция данных из файла

Если нужно перенести данные из JSON:

1. Установите переменную: `MIGRATE_FROM_FILE=true`
2. Поместите файл в `server/data/records.json`
3. Перезапустите сервер

Подробнее: см. `POSTGRESQL_DEPLOY.md`

